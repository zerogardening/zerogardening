# 통합관리 프로그램 — 기능명세

> ## 🔴 이 문서는 설계도가 아니라 **채점표**다
>
> **만드는 사람은 이걸 보지 않는다.** designer·architect·builder는 `업무규칙.md`만 본다.
> 여기에는 현행 코드의 함수 이름과 줄 번호가 적혀 있어서, 보고 만들면 **똑같은 것이 나온다.**
>
> | 누가 | 이 문서를 |
> |---|---|
> | designer · architect · builder | **안 본다** → `업무규칙.md`를 본다 |
> | **checker** | **다 만든 뒤** 빠진 기능이 없는지 대조할 때만 본다 |
>
> 재개발은 **제로베이스**다 (2026-07-30 확정). 기능은 배워오되 구현은 처음부터 한다.
> 결과가 현행과 비슷해지면 그게 맞는 답이라서 그런 것이고, 다르면 더 나은 걸 찾은 것이다.
>
> 현재 `app.html`(16,688줄)을 뜯어 역으로 정리했다. **추측이 아니라 코드에서 확인한 것만 적었다.**
> 작성: 2026-07-30 · 대상: `app.html` (v0.3)

---

## 1. 현재 구조 — 사실

| 항목 | 값 |
|---|---|
| 형태 | `app.html` **단일 파일** · 16,688줄 · 989KB |
| 함수 | 445개 |
| 배포 | GitHub Pages — https://zerogardening.github.io/zerogardening/app.html |
| 외부 라이브러리 | Supabase JS v2, SheetJS(xlsx) 0.18.5, Pretendard 폰트 |
| 데이터 | **Supabase** `app_meta` 테이블 + 브라우저 localStorage 이중 저장 |
| 화면 | 라우트 5 + 예정 2 · 하위 탭 17 · 모달 18 |

### 🔴 데이터 저장 방식 (재개발의 핵심 이유)

`app.html:4384` — **전체 데이터를 JSON 하나로 묶어 `app_meta` 테이블의 `key='state'` 한 행에 통째로 upsert**한다.

```
save(data) → localStorage에 통째로 저장 → Supabase에 통째로 upsert
```

- **동시 편집 시 나중 저장이 앞 저장을 통째로 덮어쓴다** (last-write-wins)
  - 실시간 구독(`_subscribeCloud`)이 있어 다른 창의 변경을 받아오긴 하지만, **충돌 병합은 없다**
  - 지금은 우람님 1인이라 안 터짐 · **두용씨 합류 시 즉시 위험**
- 데이터가 커질수록 저장·로딩이 통째로 느려진다
- 검색·필터·집계를 전부 브라우저가 처리 (`computeInventoryList` 등)

**새로 만들 때는 데이터 종류별로 테이블을 나눠야 한다.**

---

## 2. 데이터 종류 (14종)

`app.html:4308` `DEFAULT` 정의 기준.

| 키 | 내용 |
|---|---|
| `receivings` | **입고 기록** — 핵심 데이터. 아래 필드 참조 |
| `skus` | **품목코드 마스터** — 입고와 별개로 SKU 단위 정보 관리 |
| `companies` | 업체 (공급업체·판매처·운송·기타 + 자기회사 1건) |
| `statements` | 발행한 명세서·견적서 이력 |
| `orders` | 카페24에서 받은 주문 |
| `orderBatches` | 주문 업로드 폴더 (`260506-01` 형식) |
| `quoteRequests` | 견적 요청 접수 목록 |
| `cardPresets` | 주문카드 메모 프리셋 (내장 프리셋 포함) |
| `manualCards` | 일회성 수동 출력 카드 |
| `plantMaster` | 소싱 추천용 식물 후보 목록 |
| `keywordData` | 네이버 검색량 데이터 |
| `competitorPrices` | 수동 입력한 경쟁사 가격 |
| `sourcingRuns` | 소싱 추천 실행 이력 |
| `myPerformance` | 쿠팡 판매 리포트 |
| *(추가)* `inventoryAdjustments` | 재고 수동 조정 기록 |

### 입고 레코드 필드

`receivedAt`(입고일자*) · `supplier`(입고업체*) · `scientificName`(학명) · `distributionName`(유통명*) · `spec`(규격) · `barcode`(품목코드) · `purchasePrice`(매입단가*) · `quantity`(수량*) · `memo` · `taxable`(과세/면세)

**식물 특성(선택):** `heightMin/Max` · `widthMin/Max` · `hardiness`(USDA Zone 2~10) · `watering`(자주/보통/적게/건조/다습) · `sunlight`(양지~음지 5단계) · `bloomStart/End`(개화 월)

---

## 3. 화면 지도

### 🌿 상품 관리 (`receiving`) — 탭 4개

| 탭 | 기능 |
|---|---|
| **수동 입고** | 입고 1건 등록. 업체·학명·유통명·품목코드 **자동완성**. 규격 프리셋(10/15/18/21/27cm 포트+직접입력). 품목코드 자동생성 버튼. **🤖 AI 글쓰기 요청** 버튼 |
| **입고 리스트** | 검색(유통명·학명·품목코드·업체) · 업체별 필터 · **달력 보기** · 품목코드별 보기 · **엑셀 다운로드** · 요약바 · 규격별 분포 |
| **재고 관리** | 재입고 필요 대시보드 · 상태 필터(판매중/품절/일시중지/재입고필요) · 입고일 범위 필터 · **일괄 상태변경** · **일괄 AI 글쓰기** · **단가 일괄 조정** · 엑셀 다운로드 |
| **품목코드 관리** *(숨김 탭)* | SKU 마스터 관리 · **코드 중복 검사**(앞5자 겹치는 다른 식물 탐지) · 출처 필터 · 신규 SKU 수동 등록 |

**부가 기능**
- **📥 엑셀 불러오기** — 시트 선택 → 컬럼 매핑 → 미리보기 → 등록 (자유 양식)
- **📦 일괄 업로드** — 고정 양식(A매입일자 B코드명 C품목명 D규격 E수량 F단가 G공급가액 H입고업체)
- **🔧 기존 데이터 정리** — 규격 통일 + 품목코드 일괄 부여
- **📤 백업 내보내기 / 📥 불러오기** — JSON

### 📄 명세서 발행 (`statement`) — 탭 2개

| 탭 | 기능 |
|---|---|
| **작성** | 견적서/거래명세서 선택 · 공급받는자 자동완성 · 공급자 자동표시(업체관리에서 `isSelf` 지정한 1건) · 품목 검색 추가 + 직접 입력 · **할인/할증 %** · **단위 절삭**(10/100/1,000/10,000원 × 반올림/올림/내림) · 합계 자동계산 · **🖨 출력**(인쇄) · 💾 저장 |
| **발행 이력** | 출력 시점에 자동 저장됨 · 고객사명/NO 검색 · 유형 필터 |

**단가 기본 공식:** `(매입가 × 2) × (1 - 할인율)` → 100원 반올림

### 🏢 업체 관리 (`companies`) — 탭 2개

| 탭 | 기능 |
|---|---|
| **업체 등록** | 업체명* · 유형*(공급업체/판매처/운송택배/기타) · 대표자 · 사업자번호 · 연락처 · 이메일 · 주소 · 담당자 · 취급품목(검색 키워드) · **입금계좌**(명세서 표시) · 거래조건 · 메모 · **자기회사 지정 체크박스**(1개만) |
| **업체 검색/리스트** | 항목별 검색(전체/업체명/대표자/연락처/취급품목/주소/사업자번호) · 유형 필터 · 정렬(최근/이름/유형) |

### 🛒 주문 관리 (`orders`) — 탭 3개 + 숨은 탭 2개

| 탭 | 기능 |
|---|---|
| **일자별 주문** | CSV/Excel **드래그앤드롭 업로드** → 일자별 폴더 자동생성(`260506-01`) · 동일 파일명 시 덮어쓰기/새로저장 선택 · 폴더 탐색 → 폴더 상세 · **📋 수거 리스트(Pick List) 출력** · 폴더 삭제 |
| | **🚚 로젠 송장 → 카페24 배송완료 변환** — 로젠 엑셀 업로드 → `주문번호/공란/운송장번호/박스수량` 4열 CSV 출력 (수량 1은 공란). 저장 폴더 지정 가능(File System Access API) |
| **대시보드** | 오늘/이번주/이번달 주문건수·매출 · 인기품목 TOP10 · 최근 주문 10건 · 전체 누적 |
| **견적 요청** | 요청일자* · 고객명* · 연락처 · 요청내용* 등록 → 목록(상태: 요청/완료/취소) |
| *주문 리스트* (숨김) | 항목별 검색(주문번호/수령인/주문자/상품명/품목코드/전화/주소) · 기간 필터 |
| *CSV 업로드* (숨김) | 카페24 주문 CSV 매핑 · **중복 자동 제거**(주문번호+품목코드+수령인) · 품목코드 매칭 통계 |

**주문카드 출력** — 개별/그룹 카드 HTML 생성 · 메모 프리셋 · 수동 카드 작성 · 페이지 나누기

### 🌱 소싱 추천 (`sourcing`) — 탭 4개

| 탭 | 기능 |
|---|---|
| **AI 추천** | 타입·시즌·최소 연간검색량 조건 → **네이버 검색량 기반**으로 시즌에 수요 느는 식물 선별 · **시장 전체에서 미보유 발굴**(네이버 쇼핑) · 식물명 직접 붙여넣기 |
| **키워드검색** | 키워드 입력 → 소싱 가능성 즉시 진단 · 내 식물 전체 진단(검색량·경쟁률 불러오기, 쿠팡 리포트 업로드, 정렬 5종) · 월별 검색추이 그래프 |
| **가격 경쟁력 진단** | **타입 × 화분사이즈**로 비교 · 쿠팡 실판매 평균가(매출÷주문) + 수동 입력 경쟁가 기준 · 가격대 밴드 표 |
| **즐겨찾기** | 관심 품목 보관 |

### 🚧 예정 (미구현)
- 재고 관리 *(상품관리 탭 안에는 이미 있음 — 별도 카테고리는 미구현)*
- 고객 관리

---

## 4. 숨은 규칙 — 새로 만들 때 반드시 옮겨야 할 것

### 품목코드 생성 (`generateItemCode`, `app.html:5305`)

1. **접두 3자** = 학명에서 생성 (`genCodePrefix`)
   - 품종명이 따옴표 안에 있으면 → **속명 앞 2자 + 품종명 첫 글자** (예: `Miscanthus 'Red'` → `MIR`)
   - 따옴표 없으면 → **속명 앞 3자** (예: `Festuca` → `FES`)
2. **일련번호 2자** — 같은 학명 + **같은 규격**이면 기존 번호 재사용, 규격이 다르면 새 번호
3. **규격 접미사** — `-10`, `-15` 등

> 결과 예: `FEE01-10` · **SKU 단위 = 품목명 + 규격**. 같은 식물이라도 화분 크기가 다르면 다른 코드.

### 규격 정규화 (`CHI_TO_CM_PORT`, `app.html:5281`)

| 입력 | 저장 |
|---|---|
| 4치 | 10cm 포트 |
| 5치 | 15cm 포트 |
| 6치 | 18cm 포트 |
| 7치 | 21cm 포트 |
| 9치 | 27cm 포트 |

**「치」와 「cm」이 섞여 들어와도 전부 cm 포트로 통일 저장.**

### 가격 공식 (`DEFAULT_PRICE_FORMULA`, `app.html:13226`)

| 항목 | 기본값 |
|---|---|
| 소매 배수 | 매입가 × **1.7** |
| 도매 배수 | 매입가 × **1.2** |
| 쿠팡 배송비 | **4,500원** |
| 쿠팡 부가세 | **1.1** |
| 쿠팡 소비자가 배수 | **2** |
| 쿠팡 수량 옵션 | 1, 2, 3, 4, 6, 9 |

명세서 단가는 별도 — `(매입가 × 2) × (1-할인율)` → 100원 반올림.

### 재고 계산 (`computeInventoryList`, `app.html:13896`)

```
현재고 = 총 입고수량 − 총 출고수량(주문)
```

- **수동 조정이 있으면** 조정 시점 기준으로 다시 계산
  `현재고 = 조정값 + (조정 이후 입고) − (조정 이후 출고)`
- 집계 단위는 **품목코드 앞자리 키**(`normalizeInventoryKey`)
- 표시 이름 우선순위: **입고 유통명 → 입고 학명 → SKU마스터 유통명 → 주문 상품명**
  (주문 상품명은 쇼핑몰 제목이라 길어서 최후 수단)

### 주문 중복 판정
`주문번호 + 품목코드 + 수령인` 조합이 같으면 자동 스킵.

### 주문 폴더 ID
`YYMMDD-순번` (예: `260506-01`) — 같은 날 여러 번 올리면 순번 증가.

---

## 5. 외부 연동

| 대상 | 용도 | 비고 |
|---|---|---|
| **Supabase** | 데이터 저장·실시간 동기화 | `vjqfhwrgrocapcyndgtx.supabase.co` |
| **카페24** | 주문 CSV 다운로드 → 수동 업로드 | API 연동 아님 |
| **로젠택배** | 송장 엑셀 → 배송완료 CSV 변환 | 수동 |
| **네이버** | 검색량·쇼핑 상품수 (소싱 추천) | 호출 경로 확인 필요 |
| **쿠팡** | 옵션별 판매 리포트 업로드 | 수동 |
| **AI 글쓰기** | `ai_writing_queue/pending` 파일 큐 | 로컬 폴더 경유 |

---

## 6. 아직 안 판 것 (2차 조사 필요)

- **주문카드 출력 레이아웃** — `renderSingleCardHtml`·`renderGroupCardHtml`(11595~13000줄). 인쇄물이라 눈으로 대조해야 함
- **수거 리스트(Pick List) 정렬·묶음 규칙**
- **네이버 검색량 호출 경로** — 브라우저에서 직접인지 중계 서버인지
- **엑셀 컬럼 자동매핑 규칙** — 어떤 헤더명을 어떤 필드로 인식하는지
- **AI 글쓰기 13개 항목**의 정확한 필드 정의 (`.claude/skills/zg-ai-writing/SKILL.md` 참조)

---

## 7. 새로 만들 때 결정해야 할 것

1. **데이터 테이블 분리** — 위 14종을 각각 테이블로. 필수.
2. **인쇄물 처리** — 명세서·주문카드·수거리스트는 폰에서 인쇄 못 한다. PC 전용으로 둘지 PDF로 바꿀지
3. **파일 업로드** — 엑셀/CSV 업로드는 폰에서 거의 안 쓸 기능. 모바일 화면에서 뺄지
4. **모바일에서 진짜 필요한 것** — 밭에서 쓸 화면이 뭔지 (재고 확인? 입고 등록? 주문 조회?)

### ✅ 확정된 것

| 언제 | 무엇 | 내용 |
|---|---|---|
| 7/30 | **비상용 PC화면 스위치** | **넣는다.** 폰 화면 구석에 버튼 하나 — 누르면 PC용 전체 화면을 폰에 그대로 띄운다. 글씨는 작지만 되긴 된다.<br>**왜** — 폰 화면은 기능을 줄여 만드는데, 빠진 기능이 밭에서 필요해지면 그날 일이 멈춘다. 화면을 새로 그리는 게 아니라 기존 PC 화면을 재사용하므로 드는 값은 버튼 하나뿐이다 |
