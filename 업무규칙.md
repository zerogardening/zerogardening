# 제로가드닝 업무규칙

> **프로그램이 바뀌어도 이건 안 바뀐다.** 품목코드 만드는 법, 가격 매기는 법은
> 프로그램 사정이 아니라 회사 사정이기 때문이다.
>
> **개발팀은 이 문서만 보고 만든다.** 현행 `app.html`이 어떻게 짜였는지는 보지 않는다.
> 다 만든 뒤 빠진 기능이 없는지 대조할 때만 `기능명세.md`를 쓴다.
>
> 뽑아낸 곳: `기능명세.md` (현행 프로그램에서 역으로 정리한 것) · 2026-07-30

---

## 1. 품목코드

**같은 식물이라도 화분 크기가 다르면 다른 코드다.** 코드 하나 = 품목명 + 규격.

**만드는 법 — 세 토막을 잇는다**

| 토막 | 규칙 | 예 |
|---|---|---|
| 앞 3자 | 학명에서 딴다 | 아래 참조 |
| 일련번호 2자 | 같은 학명·같은 규격이면 **기존 번호를 다시 쓴다.** 규격이 다르면 새 번호 | `01` |
| 규격 | 화분 지름 | `-10` |

**앞 3자 뽑는 법**

- 품종명이 따옴표 안에 있으면 → **속명 앞 2자 + 품종명 첫 글자**
- 따옴표가 없으면 → **속명 앞 3자**

| 학명 | 앞 3자 | 왜 |
|---|---|---|
| *Festuca glauca* **'Elijah Blue'** | `FEE` | `FE` + 품종명 첫 글자 `E` |
| *Festuca glauca* (품종명 없음) | `FES` | 속명 앞 3자 |
| *Pennisetum alopecuroides* **'Red Head'** | `PER` | `PE` + `R` |

**전체 예:** `FEE01-10` — 앞 3자 `FEE` + 일련번호 `01` + 규격 `-10`

**중복 검사** — 앞 5자가 겹치는 다른 식물이 있으면 찾아내 알린다.

---

## 2. 규격 표기

**「치」로 들어와도 「cm 포트」로 통일해 저장한다.** 거래처마다 표기가 달라서다.

| 들어온 값 | 저장하는 값 |
|---|---|
| 4치 | 10cm 포트 |
| 5치 | 15cm 포트 |
| 6치 | 18cm 포트 |
| 7치 | 21cm 포트 |
| 9치 | 27cm 포트 |

입력할 때 쓰는 기본 선택지도 **10 / 15 / 18 / 21 / 27cm 포트**이고, 그 밖의 규격은 직접 입력한다.

---

## 3. 가격

### 판매가

| 어디 | 공식 |
|---|---|
| 소매 | 매입가 × **1.7** |
| 도매 | 매입가 × **1.2** |
| 쿠팡 소비자가 | 매입가 × **2** |

**쿠팡 추가 규칙** — 배송비 **4,500원** · 부가세 **1.1** · 수량 옵션은 **1, 2, 3, 4, 6, 9**개.

### 명세서 단가

```
(매입가 × 2) × (1 − 할인율)  →  100원 단위 반올림
```

명세서에서는 **할인·할증을 %로** 걸 수 있고, 합계에 **단위 절삭**을 적용할 수 있다.
절삭 단위는 10 / 100 / 1,000 / 10,000원이고, 각각 **반올림·올림·내림**을 고른다.

---

## 4. 재고

```
현재고 = 총 입고수량 − 총 출고수량(주문)
```

**수동으로 조정한 적이 있으면 그 시점부터 다시 센다.**

```
현재고 = 조정값 + (조정 이후 입고) − (조정 이후 출고)
```

- 세는 단위는 **품목코드 앞자리**다 (규격이 달라도 같은 식물로 묶어 본다)
- 상태는 **판매중 · 품절 · 일시중지 · 재입고필요** 넷

**화면에 뭘 이름으로 보여줄지 — 이 순서로 찾는다**

1. 입고할 때 적은 **유통명**
2. 입고할 때 적은 **학명**
3. 품목코드 마스터의 **유통명**
4. 주문서의 **상품명** ← 쇼핑몰 제목이라 길다. 앞의 셋이 다 없을 때만

---

## 5. 주문

**같은 주문이 두 번 들어오는 걸 막는 법** — **주문번호 + 품목코드 + 수령인**이 모두 같으면 이미 있는 주문으로 보고 건너뛴다.

**주문 묶음 번호** — `YYMMDD-순번` (예: `260506-01`). 같은 날 여러 번 올리면 순번이 올라간다.

**로젠 송장 → 배송완료 변환** — 로젠에서 받은 엑셀을 네 칸짜리로 바꿔 내보낸다.

| 1칸 | 2칸 | 3칸 | 4칸 |
|---|---|---|---|
| 주문번호 | (비움) | 운송장번호 | 박스수량 |

**박스수량이 1이면 비워둔다.**

---

## 6. 다루는 데이터 — 14종

| 무엇 | 내용 |
|---|---|
| **입고 기록** | 가장 중심이 되는 데이터 |
| 품목코드 마스터 | 입고와 별개로 품목 단위 정보를 관리 |
| 업체 | 공급업체 · 판매처 · 운송택배 · 기타 + **자기회사 1건** |
| 명세서 이력 | 발행한 명세서·견적서 |
| 주문 | 카페24에서 받은 것 |
| 주문 묶음 | 업로드 단위 폴더 |
| 견적 요청 | 접수 목록 (요청 / 완료 / 취소) |
| 주문카드 메모 | 자주 쓰는 문구 모음 |
| 수동 카드 | 일회성 출력용 |
| 식물 후보 목록 | 소싱 추천에 쓰는 것 |
| 검색량 데이터 | 네이버 |
| 경쟁사 가격 | 손으로 넣은 것 |
| 소싱 추천 이력 | 돌린 기록 |
| 판매 리포트 | 쿠팡 |
| 재고 수동 조정 | 조정한 기록 |

**자기회사는 업체 목록에 딱 1건만 지정한다.** 명세서의 공급자 칸이 여기서 자동으로 채워진다.

---

## 7. 입고할 때 적는 것

**반드시 적는 것** — 입고일자 · 입고업체 · 유통명 · 매입단가 · 수량

**같이 적는 것** — 학명 · 규격 · 품목코드 · 메모 · 과세/면세 구분

**식물 특성 (선택)**

| 항목 | 값 |
|---|---|
| 다 자란 키 | 최소~최대 |
| 폭 | 최소~최대 |
| 내한성 | USDA Zone 2~10 |
| 물주기 | 자주 · 보통 · 적게 · 건조 · 다습 |
| 햇빛 | 양지 ~ 음지 5단계 |
| 개화기 | 시작 월 ~ 끝 월 |

**자동완성이 되어야 하는 칸** — 업체명 · 학명 · 유통명 · 품목코드.
같은 걸 매번 다시 치지 않게 하는 것이 입고 화면의 핵심이다.

---

## 8. 절대 어기면 안 되는 것

| 규칙 | 왜 |
|---|---|
| **매입·주문 기록은 지우지 않는다** | 세금계산의 근거다 |
| **명세서는 출력한 시점에 자동 저장한다** | 나중에 "그때 얼마에 줬더라"를 찾을 수 있어야 한다 |
| **품목코드는 한 번 부여하면 바꾸지 않는다** | 주문·재고·명세서가 전부 이 코드로 이어져 있다 |
| **여러 사람이 동시에 고쳐도 서로 덮어쓰지 않아야 한다** | 지금 프로그램의 가장 큰 결함이다. 두용씨 합류 시 즉시 사고가 난다 |
