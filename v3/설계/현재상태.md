# v3 현재 상태 — 다음 창이 여기부터 읽는다

**2026-08-05 기준**

## 🔴 동봉카드 ⚙설정 + 수동 카드 작성 끝났다 — 검수 전, 배포 컨펌 대기

우람님 지시 두 건을 붙였다. **새 탭·새 페이지는 만들지 않았다** — 전부 `💌 동봉카드` 창(`.pcsheet`) 안에서 해결.
① `⚙` 버튼 — 안내말씀 프리셋(추가·수정·삭제, 내장 3개는 삭제 잠금) · 인사말 두 줄 · 상단 태그라인 · 종수별 글자수 상한을 화면에서 고친다. 새 파일 `js/09b-카드설정.js`. 저장키 `ZG.저장소.키.동봉카드설정`(localStorage `zg.v3.동봉카드설정`, 객체 하나) — 값이 없으면 지금 하드코딩 값이 그대로 기본값이라 기존 동작이 안 깨진다.
② `✎ 수동 카드` 버튼 — 주문 없이 품목만 골라(자동완성, `08c-주문입력.js`의 `품목카드묶음` 재사용) 카드를 뽑는다. 받는분·송장번호·주문번호 없음. 일반 카드와 같은 `판만들기()`를 타 모양이 동일.
`js/09-동봉카드.js`는 상수 대신 설정을 읽게 바뀌었고 `열기(건들)` 기존 시그니처는 안 건드림.
Node가 Playwright + 실제 Chrome으로 직접 클릭·타이핑해 재확인 완료 — 설정 반영·삭제잠금·새로고침 후 유지·수동카드 생성·폰 390px 가로 오버플로우 0·콘솔 에러 0건.
검수 1차 🔴 1건(삭제 확인창에서 Esc 누르면 설정창까지 통째로 닫혀 입력값 소실 — `08c`·`09`·`09b` 세 모듈이 각자 document에 Esc 리스너를 걸어 중복된 게 원인) → dev-builder가 `js/04-공통UI.js`에 단일 Esc 등록기(`탈출걸기`/`탈출풀기`)를 만들어 통일 → 재검수 🔴 0건, 회귀(출고리스트·수동주문 등) 없음 확인.
검수서 `v3/검수/09b-카드설정-검수.md`.
🟡 배포 컨펌 때 함께 여쭐 것 — 내장 프리셋 3개 중 「📋 원래 안내글」과 「🌤️ 계절·날씨 안내」가 어미만 다른 사실상 같은 문구(2개로 줄일지 3개로 둘지).
🔴 **검수까지 끝났다. 다음은 우람님 컨펌 → push.**

## 🔴 동봉카드 시안대로 재구현 끝났다 — 검수 🔴 0건, 배포 컨펌 대기

시안(`v3/시안/09-동봉카드.html`) 확정본대로 `js/09-동봉카드.js`·`css/주문.css`(228행~ `.encard` 계열)를 다시 짰다. 새 폴더 `v3/폰트/`(나눔바른펜 R·B)·`v3/img/`(카드로고1.png) — 시안 것 복사, 원본은 안 지움. 시안 픽셀 대조 차이 0.0001~0.0037%(안티앨리어싱 수준), 폰트·로고 md5 동일. 인쇄 제목 버그도 `.pcsheet:has(.encard) .hd` 방식으로 고쳐 출고리스트 제목은 안 건드림. 검수서 `v3/검수/09-동봉카드-검수.md`.
🟡 5건 배포 컨펌 때 함께 여쭙는다 — ①관리특이사항이 길면(3종 100자 근처) 인사말·안내말씀이 잘림 표시 없이 통째로 사라짐 ②특성값 11자 넘으면 4·5·6종에서 옆 항목과 겹쳐 찍힘 ③특성 없는 품목은 빈 상자로 인쇄 ④안내 말씀이 코드에 박힌 두 문장뿐이라 우람님이 못 고침(카드 메모 프리셋 없음) ⑤수동 카드 작성 기능 없음.

## 🔴 주문탭 수정 10건 끝났다 — 검수 통과, 배포 컨펌 대기

시안 없이 바로 코드로 갔다(우람님 지시 — 10건이 이미 확정돼 그림으로 새로 판단할 게 없었다). 폰 수동주문 여러 품목 카드 등록(화분크기 고정 드롭다운 10/15/18/21/직접입력) · 폰 상세 군더더기 제거 · 수동 건만 수정·삭제 · PC 수동주문 수정창 · **동봉카드 실제 구현**(6종 2×3 · 7종+ 쪽나눔 · 출력번호 숫자만).
고친 파일 `js/08c-주문입력.js` · `js/08b-주문목록.js` · 새 파일 `js/09-동봉카드.js` · `주문.html` · `css/주문.css`.
검수 1차 🔴 3건(고정규격 밖 코드 침묵오류·단가에 매입가 들어감·규격변경시 단가 0원화) → dev-builder가 `08c` 재수정 → 재검수 🔴 0건.
🔴 **그 뒤 Node가 직접 열어봐서 검수가 놓친 3건을 더 찾았다** — 폰 스테퍼가 화면 밖으로 나가 못 눌렀던 것(🟡로 낮춰 적었던 게 실은 🔴) · 동봉카드 1~2종일 때 아래가 텅 비던 것 · 특성 6개가 세로 1열로 쌓이던 것(3열이어야 함). **콘솔 에러 0건만 보고 화면 실물을 안 본 게 원인** — `dev-checker.md`에 이미 있는 "한글은 직접 타이핑" 규칙과 같은 종류 사고. `css/주문.css`·`js/09-동봉카드.js` 재수정 → 스크린샷으로 직접 확인 → 재검수 🔴 0건.
검수서 `v3/검수/주문-수정10건-검수.md`, 스크린샷 `v3/검수/샷-0805재검수/`(25장). 또한 **`js/00-시드.js`·`js/01-저장소.js`를 Node가 고쳤다**(목데이터 품목 `특성`이 통째로 null이라 카드 속이 비어 보였던 것 — 특성표 채우고 스키마버전 2→3).
🟡 남은 것(27cm 드롭다운 미포함·인쇄 첫 쪽에 창 제목 같이 찍힘·마스터에 없는 규격 안내 없음·시안 03-주문.html 미갱신)은 배포 컨펌 때 함께 여쭙는다.

**시안 파일 `08e-수동주문-phone.*`·`09-동봉카드-*.*`는 전부 지웠다** — 코드가 곧 실물이라 더 안 만든다. `시안/03-주문.html`은 옛 설계라 지금 화면과 다르다(우선순위 낮음).

---

## 1단계 끝났다 — 상품관리(입고·재고) + PWA

시안 → 설계 → 구현 → 검수 → 버그수정까지 완료. **검수 🔴 0건, 아이폰 실기기 확인까지 마쳤다.**

```
통합관리/v3/
  index.html · manifest.json · icons/
  css/공통.css · css/상품.css
  js/00~07 (15개)
  설계/1단계-설계.md    ← 데이터 구조·규칙의 원본
  시안/                  ← 확정 디자인
```

### 여는 법
```bash
cd /Users/zerogardening/claude-projects/제로가드닝-개발/통합관리/v3
python3 -m http.server 8777
```
→ `http://localhost:8777/index.html?reset=1` · 아이폰은 `ipconfig getifaddr en0`으로 나온 주소:8777 (**공유기가 주소를 바꾼다 — 매번 확인한다**)

---

## 🔴 확정된 것 — 새 화면은 여기에 맞춘다

| 무엇 | 어디 |
|---|---|
| **디자인 = macOS A안 (브랜드 초록 `#2F9E44`)** | `시안/_공통-macA.css` · `시안/00-뼈대-macA.html` |
| 만들어진 화면 | `시안/01-입고-macA.html` · `시안/02-재고-macA.html` |
| 데이터 구조·규칙 | `설계/1단계-설계.md` |

- 골격은 애플, **액센트만 회사 색**. 초록이 칠해지는 곳은 저장 버튼·왼쪽 메뉴·품목코드 세 자리뿐
- 상태 배지는 **무채색(정상) + 빨강(급한 것)** — 액센트와 겹치지 않게 뺐다
- 애니메이션 값은 CSS 변수(`--ease-ios` `--dur-tap` 등). **한 곳에서 조절한다**
- 접기 부품은 **하나만 쓴다**(`fold`/`foldbody`) — 입고 「식물 특성」, 재고 「재입고 요청」이 같은 부품
- 폰은 밭에서 **등록·수정까지** 한다. 보기 전용이 아니다

---

## 아직 안 한 것

### 🔴 첫 push — 우람님 판단 대기
올리면 `https://zerogardening.github.io/zerogardening/v3/` 로 열린다.
**실서비스 `app.html`과 주소가 달라 병행해서 쓸 수 있다.** 아이폰에서 홈 화면에 추가하면 앱이 된다.
8/3부터 남은 **저장소 백업 미반영 건**(v3 시안·업무규칙·기능명세)도 이때 같이 정리한다.

### 🟡 급하지 않은 것
- 재고조정을 한 번이라도 한 품목은, 그보다 오래된 입고를 고쳐도 재고가 안 바뀐다 — **업무규칙에 그렇게 정의돼 있어 맞는 동작.** 삭제 확인 문구만 다듬으면 된다
- 입고 내역 달력 보기·품목코드별 보기, 재고의 입고일 범위 필터 — 미구현
- `업무규칙.md` §4가 「어느 시점 기준인지」를 안 밝힘 — 문서 정정 필요
- `업무규칙.md`와 구현이 어긋난 곳 하나 — 같은 식물·다른 규격일 때 일련번호 재사용. **구현이 실데이터와 맞으니 문서를 고친다**

---

## 2단계 주문탭 끝났다 — 설계 → 구현 → 검수(🔴 0건)

`주문.html` + `js/08a~08d-주문*.js` + `css/주문.css` + `js/lib/xlsx.min.js`(SheetJS 로컬 사본). 설계 `설계/2단계-설계.md`(212줄), 검수 `검수/2단계-검수.md`.
`index.html` 사이드바/하단탭 「주문」 메뉴 연결까지 확인 완료. **1단계(입고·재고)는 이번 변경으로 안 깨졌다.**

## 8/5 — 우람님 수정 10건까지 반영 끝났다

🔴 **아래 「끝났다」 절의 낡은 내용을 이 절이 덮는다. 여기가 최신이다.**

| 무엇 | 어디 |
|---|---|
| **폰 수동주문 — 품목 여러 장** | `js/08c-주문입력.js`. 품목명 자동완성 + **화분 사이즈 고정 드롭다운(10·15·18·21·직접입력)** + 수량. **단가·금액 칸은 없앴다** |
| **수동 건만 수정·삭제** | `출처:'수동'` 인 줄만 연다. 카페24·스토어 건은 🔒 |
| **동봉카드 — 진짜로 돌아간다** | `js/09-동봉카드.js` 신설. 받는분·송장번호 줄 없음 · 하단 로고 없음 · **우측 아래 출력번호** · 1~6종 판이 각각 다르고 **빈 공간 없이 채운다** · 7종부터 쪽나눔 |
| 시안 파일 | **안 만든다.** 우람님이 「시안 없이 바로 코드」로 정하셨다. 돌아가는 화면이 곧 컨펌 대상이다 |

**Node 가 직접 고친 것** — 검수자가 놓친 것들이다
- `js/00-시드.js` **목데이터 특성이 통째로 `null` 이라 동봉카드가 텅 빈 채로 떴다.** 6종 채우고 `js/01-저장소.js` 스키마버전 2→**3**
- `js/08a`·`08b` **회차 칩이 조회 시작일 하루치만 보이던 것** → 기간 전체. 여러 날이면 `08-04 1회차 09:10` 처럼 날짜가 붙는다
- `css/주문.css`·`js/08c` **창 없이 Ctrl+P 하면 백지가 나오던 것** → `body.인쇄중` 이 붙었을 때만 인쇄 CSS 가 걸린다
- `css/주문.css` 특성 값이 길면 라벨 아래로 떨어져 줄이 어긋나던 것

🔴 **`회차글` 이라는 함수가 이미 있는데 같은 이름을 또 만들어 화면이 깨진 적이 있다(8/4). 쓰기 전에 `grep` 한다.**

### 엑셀 변환 — 규칙은 다 나왔다. 구현만 남았다
실물 양식 4종을 `백업/` 에 받았고, **`app.html` 9538~9975줄에 이미 돌아가는 로직이 있다**(우람님 지시로 찾아 옮긴다).
🔴 **규칙 원본은 `설계/엑셀변환-규칙.md`. 거기부터 읽는다** — 유통명 치환·`수량=N개`만 곱하기·박스운임 자동계산·**작업자 포장순서 정렬**·출력번호 `(12/★3)`·머리글 유무.

### 여는 법
```bash
cd v3 && python3 -m http.server 8777
```
🔴 **`http://localhost:8777/주문.html?reset=1`** — `?reset=1` 을 안 붙이면 옛 목데이터가 남아 **동봉카드가 빈 채로 뜬다.**

### 🟡 배포 컨펌 때 함께 여쭐 것
**남은 것** — 27cm 가 사이즈 드롭다운에 없다(직접입력으로는 된다) · 동봉카드 인쇄 첫 쪽에 화면용 제목이 같이 찍힌다 · 마스터에 없는 규격으로 바꿀 때 안내가 없다 · 시안 `03-주문.html` 이 옛 버전(그림일 뿐 동작 무관) · 주문번호로 검색 안 됨 · 자동완성 코드 접두 5자만 비교 · 주문묶음 순번 자동증가 없음 · 업로드 정규화(꼬리 `(N)`) 없음 · 대시보드·견적요청 탭이 어느 단계 몫인지 미정 · 폴더 덮어쓰기/삭제 미구현
**PC 서비스품목 일괄지정**은 아직이다(우람님 지시 ④).

### 우람님 확정 사항 — 여기에 맞춰 그린 것이다. 다시 여쭙지 않는다

**앱(폰) = 조회·인쇄·신규입력만. 편집은 없다**
- 일자 선택으로 그날 전체 · **기본은 접속 당일** · 찾기(이름·전화번호·상품명)
- 주문카드에 **주문한 사이트 표시**(쿠팡·스마트스토어)
- 🔴 **주문서 「고치기」는 통째로 삭제** — 단 **8/5에 예외가 생겼다: 손으로 넣은 건(`출처:'수동'`)만 고치고 지운다.** 카페24·스토어에서 들어온 건은 그대로 못 고친다
- **동봉카드 인쇄** · **전화주문·개별주문 신규 입력**(이제부터 쓰신다)

**웹(PC) = 이 다섯이 주문탭의 전부다**
① 카페24 주문 엑셀 → **로젠 변환** ② 조회·검색 ③ **동봉카드** ④ **출고리스트** ⑤ 로젠 출력완료 → **카페24 배송완료 변환**

**규칙**
- 표 한 줄은 **품목 단위**. 화면에서는 **「이름＋주소＋전화번호」가 같으면 한 건으로 묶어** 보여준다 (주문번호 아님)
- **쿠팡 옵션 수량은 곱한다** — 주문 2 × 옵션 4 = **총 8개**
- 🔴 **주문 상태 절차 없음** — 신규/출고준비/출고완료 4단계 폐기. 상태 칩·일괄 상태변경 없다
- 재고 부족은 **막지 않는다** — 출고리스트에 「재고부족」 + 주문카드 **빨간색** 표시만
- **단가는 주문서에 적힌 금액 그대로** (매입×1.7 자동계산 아님) — 단 **수동주문은 단가를 아예 안 받는다**(8/5). 「주문정보 입력이라 품목·수량만 있으면 된다」 하셨다. **금액은 다음 단계 명세서에서 채운다**
- **합포장 묶음키는 「이름＋우편번호＋연락처」다**(8/5). 확정문의 「주소」를 우편번호로 바꿨다 — 주소 글자를 그대로 비교하면 띄어쓰기 하나로 다른 사람이 된다. `050` 안심번호는 달라도 같은 사람으로 묶는다

### 🔴 2단계에서 만들지 않는 것

**동봉카드 폰→사무실 프린터 원격출력.** 카드 화면 자체는 8/5에 만들었지만, **아이폰에서 사무실 프린터로 직접 보내는 것**은 별건이다.
아이폰에서 사무실 프린터로 직접 못 보낸다(브라우저 제약, 구글 클라우드 프린트 2020 종료).
되게 하려면 **사무실 PC 상주 프로그램 + Supabase 인쇄 대기열**이 필요하다 — 주문탭이 끝난 뒤 별건으로 여쭙는다.

🔴 **`기능명세.md`는 검수자만 본다.** 순서: `공통뼈대 → 상품관리 → **주문** → 명세서 → 업체 → 소싱`.

---

## 다음 창이 알아야 할 사고 하나

**헤드리스 검수로는 한글 입력 버그를 못 잡는다.**
8/4에 「휴케라」가 `ㅎㅠㅋㅔㄹㅏ`로 깨지는 걸 우람님이 아이폰으로 잡으셨다. 검수는 「콘솔 에러 0건」이라 했지만 **한글을 한 번도 안 쳐봤다.**
`dev-checker.md`에 「한글 입력칸은 실제로 타이핑해본다」를 영구 규칙으로 넣었다. **그래도 실기기 확인은 우람님 몫으로 남는다.**
