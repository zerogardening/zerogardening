/* 06f-블로그 — 「상품」 화면 네 번째 탭. 이번 주 블로그 원고를 여기서 고치고 복사한다.
   06d(이미지)와 같은 갈래다 — 자료를 받아오지 않고, 맥에서 도는 편집기(상품/_도구/편집.py)
   창만 얹는다. 원고 파일은 `블로그/원고/2026-W35/…` 라 저장소 밖이고, 웹에서는 못 읽는다.

   🔴 편집기가 꺼져 있으면 창이 빈다. 그때는 맥 터미널에 「상품이미지」 한 낱말이다
      — 이미지 탭과 같은 편집기다(포트도 같다). */
window.ZG = window.ZG || {};
(function (ZG) {
  'use strict';

  var u = ZG.ui, 만들기 = u.만들기;
  var 편집기 = 'http://127.0.0.1:8765/블로그';

  function 머리() {
    return { 제목: '블로그 원고', 왼: '주차별 원고 · 네이버에 붙일 본문', 오: '' };
  }

  function 그리기(본문) {
    u.비우기(본문);

    /* 폰에서는 안 연다 — PC 편집기 창을 얹는 자리다 (06d와 같다) */
    if (u.폰인가()) {
      var 말 = 만들기('div', { class: 'img-덧 img-폰' });
      말.innerHTML = '이 탭은 <b>PC에서</b> 쓰십니다.<br>' +
                     '원고를 길게 놓고 고치는 자리라 폰에서는 좁습니다.';
      본문.appendChild(말);
      return;
    }

    var 덧 = 만들기('div', { class: 'img-덧' });
    덧.innerHTML = '아래가 비어 있으면 편집기가 꺼져 있는 것입니다 — ' +
                   '맥 터미널에 <b>상품이미지</b> 한 낱말을 치시고 이 탭을 다시 누르십시오. ' +
                   '🔴 <b>고치던 것은 저장하고 탭을 옮기십시오</b> — 탭을 바꾸면 창이 새로 뜹니다.';

    var 틀 = 만들기('div', { class: 'img-틀' }, [
      덧,
      만들기('iframe', { class: 'img-창', src: 편집기, title: '블로그 원고',
                  allow: 'clipboard-write' })
    ]);
    본문.appendChild(틀);
  }

  ZG.블로그 = { 그리기: 그리기, 머리: 머리 };
})(window.ZG);
