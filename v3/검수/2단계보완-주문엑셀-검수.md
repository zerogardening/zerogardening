# 2단계 보완 — 카페24 주문 올리기 → 로젠 대량등록 파일 · 검수

2026-08-08 · 검수자(checker) · 대상 커밋 `69c1dc3` · `v3/주문.html?v=24`
새 파일 `08h-주문올리기.js` `08i-로젠파일.js` · 고친 파일 `08d` `08b` `09` · html 5개
채점표 — `~/.claude/plans/curried-giggling-snowflake.md` §6(위험) · §7(검수)

검수 방법 — **헤드리스가 아니라 실제 Chrome을 CDP로 몰았다.**
`http://localhost:8778/v3/주문.html` (로컬 http 서버 · Supabase CDN 차단 → 로컬 전용)
파일은 전부 **진짜 `File` 을 `DragEvent('drop')` 에 실어** 드롭존에 떨궜다 — `08d.읽기()` 경로를 그대로 지난다.
내려받은 xlsx 는 `zipfile` 로 풀어 셀을 하나하나 읽었다(화면 문구를 믿지 않았다). 샷은 `샷-2단계보완-주문엑셀/`.

## 판정 — 🔴 1건 · 🟡 6건 · 🟢 4건 → **반려**

계산식(박스점수·운임·옵션입수·L/Z열·우편 zero-pad·날짜)과 중복 판정은 **한 군데도 틀리지 않았다.**
실물 짝 대조에서 **54칸 중 52칸이 한 글자도 안 달랐다.** 남은 2칸 중 하나가 아래 🔴다.

---

## 🔴-1 · 실물 짝 대조 — E2 가 「원본 유지」가 아니다 (채점표 §7 최종 관문 불통과)

`백업/카페24주문원본_20260806.csv`(2줄)를 그대로 올려 저장하고 로젠 파일을 뽑아
`백업/로젠대량등록_20260806-01.xlsx` 와 **27칸 × 2줄 = 54칸** 을 기계로 대조했다.

```
시트명 송장출력 == 송장출력 · 행수 2 == 2 · 머리글 없음(A1이 데이터) · 27칸 전부 문자열
  ✗ E1  기준=[(★2) 추명국 핑크터치(15cm) 1개]
        새것=[(1/★2) 추명국 핑크터치(15cm) 1개]          ← 계획서 §6-5 대로. 맞다
  ✗ E2  기준=[제로가드닝 추명국(아네모네) 휠 윈드 핑크 15cm화분 노지월동 야생화 정원식물]
        새것=[(2) 제로가드닝 추명국(아네모네) 휠 윈드 핑크 15cm화분 노지월동 야생화 정원식물]
다른 칸 수: 2 / 54
```

나머지 **52칸은 한 글자도 안 달랐다.** 확인한 칸 — `J=2750`(두 줄 다) · `L1=18100` `L2=18150` ·
`Z=0`(빈칸→0) · `O1=41007` `O2=03022`(**앞 0 생존**) · `K`·`Q` 빈칸 유지 · `AA=무료`.

**왜 🔴인가** — 채점표 §7 「최종 관문」이 **`(★2)`→`(1/★2)` 하나만 달라야 하고 나머지 26칸 × 2줄이
한 글자도 안 달라야 한다 · **2행은 코드 없어 원본 유지** · 어긋나면 배포하지 않는다」로 못 박았다.
계획서 §0 실물대조도 **「H코드가 없어 유통명을 못 찾음 → 지어내지 않고 원본 유지」**다. 실제 결과는 `(2) ` 가 붙었다.

**원인** — `08i-로젠파일.js:170`

```js
o[C.상품명] = (j === 0 ? 앞머리(g.출력번호, 회) : '') + 상품글(것) + (여럿 ? '/' : '');
```

`앞머리()`가 **품목코드 유무와 무관하게** 묶음 맨 앞 줄이면 무조건 붙는다.
코드가 없는 줄이 혼자 한 묶음이면 그 줄이 곧 맨 앞 줄이라 `(N) ` 이 붙는다.

**🔴 그냥 떼면 안 된다 — 판단이 필요하다.**
앞머리를 코드 없는 줄에서 빼 버리면 **그 송장에는 출력번호가 사라진다.** 그런데 §4 는
「로젠 E열의 `(12)` 와 동봉카드 오른쪽 아래 회색 `12` 가 같은 숫자가 된다」를 이 기능의 목적으로 삼았다.
실물 검증에서도 코드 없는 줄(정민수·한서영)에 카드가 **실제로 만들어졌다**(번호 7·10).
번호를 떼면 그 카드는 짝이 없어진다.
→ **계획서 §0(원본 유지)과 §4(모든 묶음에 출력번호)가 서로 어긋난다.** builder 가 §4 를 골랐고 그렇게 골랐다는 말을
자체보고에는 적었지만 **커밋 메시지에는 「전부 일치(E열 출력번호만 규칙문서대로 추가)」로 적었다** — 실제로는 E2 도 달라졌다.
`dev-manager` 가 우람님께 여쭙고 정하실 일이다. 정해지기 전에는 **배포하지 않는다.**

---

## §7 검수표 9개 — 전부 돌렸다

함정 CSV `샷-2단계보완-주문엑셀/샘플_함정.csv`(머리글 1행 + 27열 · 데이터 14줄 + 빈 줄 1)
— 주문일 `5/13/26`·`2026-08-08 09:12:33`·`작년 여름` / 코드 `ech01-10`·`ERB01-10(2)`·`Ruellia simplex`·**빈칸 2줄(같은 주문·다른 상품)** /
옵션 `수량=4개`+M열2+코드꼬리(3) · `색상=3.그린` / **050 안심번호가 서로 다른** 같은 사람 2줄 / 우편번호 빈칸 /
지난주문과 같은 사람(★2) / 포트 13점 묶음 / 주문번호 빈 줄.
EUC-KR 사본은 `iconv` 대신 파이썬 `encode('euc-kr')` 로 만들었다(같은 바이트).

| # | 무엇 | 기대 | **실측** | |
|---|---|---|---|---|
| 1 | 올려 미리보기 | 숫자 일치 · **저장 전 줄 수 불변** | 힌트 `13줄 읽음 (무시 1줄)` · 들어올 **12건(주문 10건)** · 이미 **0** · 품목 못 찾음 **3** · 발주일 못 읽음 **1** · `zg.v3.주문` **23 → 23** · `zg.v3.주문묶음` **2 → 2** | ✅ |
| 2 | 저장 | 회차 칩 · 묶음 1줄 | 주문 **23 → 35**(12줄) · 묶음 **1줄만** `{"id":"260808-03","회차":3,"줄수":12,"건너뛴수":1}` · 창 뒤 표에 **`3회차 14:34` 칩 등장**(저장 뒤 다시그리기 ✅) | ✅ |
| 3 | 같은 파일 또 올림 | 전부 「이미」 · 새 묶음 X · 로젠 버튼 O | 이미 **12건** · 들어올 **0** · 주문 **35 그대로** · 묶음 **3 그대로** · 버튼이 `로젠 파일 만들기` 로 바뀌고 `⬇ 로젠 대량등록 파일 내려받기` **그대로 뜸** · 파일명 `20260808-03.xlsx` 로 옛 묶음 되찾음 | ✅ |
| 4 | EUC-KR 사본 | 1과 한 건도 안 다름 | 요약·상자 문구·표 **문자열까지 완전 동일** · `U+FFFD` **0개** · 한글 안 깨짐 | ✅ |
| 5 | 로젠 출력완료 파일을 잘못 넣음 | 저장 X · 「못 찾은 칸」만 | 토스트 `카페24 주문 파일이 아닌 것 같습니다 — 못 찾은 칸: 주문번호 · 발주일 · 수령인 · 수량` · 창 제목 `주문 올리기` 그대로 · 주문/묶음/출고 **전부 불변** | ✅ |
| 6 | 로젠 파일 내려받기 | 아래 표 | `20260808-03.xlsx` **13줄 · 10묶음** 생성 · 27칸 검증 통과 | ✅ |
| 7 | 동봉카드 | 순서·번호가 6의 E열 `(N)` 과 같음 | 카드 번호 **1,2,4,5,6,7,8,9,9,10** — E열과 **순서·숫자 일치** (3은 저장 안 된 줄이라 카드 없음) | ⚠️ 🟡-1·2 |
| 8 | 6을 한 번 더 | 번호 안 바뀜 | 재업로드 → 재발행 → 내려받기. `[id,출력번호]` 35줄 **전부 동일** (`출력번호 안 바뀜? True`) | ✅ |
| 9 | 6의 파일을 🚚 배송완료에 | 회귀 없음 | 아래 | ✅ |

### 6 — 내려받은 파일 27칸 (13줄 전부 읽어 확인)

```
1  (1) 휴케라 '팰리스퍼플'(15cm) 1개        J=2750   L=15000  O=48058
2  (2) 층꽃 '스털링실버'(18cm) 1개          J=2750   L=14000  O=       ← 우편 빈칸 → 혼자 처리
3  (3) 청사초(10cm) 1개                     J=2750   L=7000   O=04524
4  (4/★2) 에키네시아 '판테온'(10cm) 1개     J=2750   L=12000  O=12345  ← ★재구매 2회차
5  (5) 에키네시아 '판테온'(10cm) 8개        J=2750   L=20000            ← 수량=4개 × M열 2 = 8 (꼬리(3) 무시)
6  (6) 루드베키아(10cm) 6개                 J=2750   L=9000             ← 꼬리(2) × M열 3 = 6
7  (7) 제로가드닝 루엘리아                  J=2750   L=8000             ← 코드 못 찾음 → 원본 상품명
8  (8) 억새(27cm) 3개/                      J=확인요망                  ← 4×3 + 1 = 13점 ≥ 12
9      털수염풀(10cm) 1개/                  J=확인요망                  ← 그룹 전 줄 같은 값 · 앞머리 없음
10 (9) 수크령 '레드헤드'(15cm) 1개/         R=S=0504-2222-0002          ← 050 두 개가 한 송장 · 마지막 줄 번호로 통일
11     청사초(10cm) 1개/                    R=S=0504-2222-0002
12 (10) 제로가드닝 무늬사초 아우레올라/     L=11000                     ← 코드 빈칸 2줄이 한 주문으로 안 삼켜짐
13      제로가드닝 억새 레드치프/           L=13000
```

- **머리글 없음** ✅ (A1 이 데이터) · **시트명 `송장출력`** ✅ · **27칸 전부 문자열**(`t="str"`) ✅
- 정렬 = 포장갈래(단품15+수량1 → 단품15+수량2+ → 단품그외 → 복수건) → cm → 코드 앞5자 → 수량 → 주문번호 — **손으로 계산한 순서와 한 자리도 안 틀렸다**
- `색상=3.그린` 을 수량 3 으로 오독하지 않았다(1개) ✅ — 위험 ②(8배) 안 남
- 우편번호 없는 줄이 남과 안 섞였다 ✅ · 안심번호 그룹 통일 ✅

### 9 — 배포된 ③→④ 회귀 (`08f`·`08g`)

**먼저 파일이 실제로 안 바뀌었는지 확인했다.** `7bb9401`(직전 배포) 대비 shasum 비교 —
`08f-배송완료.js` · `08g-배송완료창.js` · `css/주문.css` · `08a` · `01-저장소` · `04-공통UI` · `02` · `00-시드` · `08c` · `08e` **전부 동일**.
`08d.시트배열(파일, 그때)` 은 3번째 인자를 안 주면 `{type:'array'}` 로 종전과 완전히 같다.

| 무엇 | 8/6 검수 때 | **오늘** | |
|---|---|---|---|
| 실물 `주문등록_출력(복수건)_출력완료(5)건 …xlsx` | 5줄 · 무시 1줄 · 정상 0 / 확인 필요 5 | **5줄 (무시 1줄) · 정상 0건 / 확인 필요 5건** · 재고 6품목 불변 | ✅ 글자 그대로 같다 |
| 로젠 **대량등록** 파일(6에서 뽑은 것)을 배송완료에 | — | 토스트 `로젠 「출력완료」 파일이 아닌 것 같습니다 — S열이 주문번호가 아닙니다.` · 출고 43 → **43** · 재고 6품목 불변 | ✅ |

---

## 함께 본 것

| 항목 | 결과 |
|---|---|
| **콘솔 에러** | **0건.** 전 시나리오 통틀어 뜬 것은 내가 CDN 을 일부러 막아 나온 `supabase-js 를 못 불러왔습니다` 경고뿐 |
| **`.needbox` 0건이면 안 그려짐** | 함정 없는 깨끗한 CSV 1줄 → needbox **1개**(들어올 줄)뿐. 품목 못 찾음·이미 있음·발주일 상자 **안 그려짐** ✅ |
| **Esc 하나에 창 하나** | 배송완료 결과창에서 Esc → `.pcsheet` 0 · `.pcscrim` 0. 주문 올리기 창에서 Esc → 1 → 0 ✅ |
| **저장 뒤 다시그리기** | 회차 칩 `3회차 14:34` 즉시 등장 · 주문 표 갱신 ✅ |
| **`zg.v3.보낼것` 에 새 줄만** | 저장 → **주문 12 + 주문묶음 1**(전체 35줄이 아니다) · 큐를 비우고 내려받기만 → **주문 12**(출력번호가 바뀐 줄만) ✅ |
| **PC 전용** | 390×844 에뮬레이션 → `.headbtns` **없음** · 본문에 「주문 올리기」 글자 **없음** · 폰 버튼은 `＋수동주문 · 📋출고리스트 · 💌동봉카드` 그대로 ✅ |
| **한글 조합** | 이 창엔 글자 칸이 없다(드롭존뿐). `08b` 를 건드렸으므로 주문조회 검색창을 `Input.imeSetComposition` 으로 `ㅎ→하→한→…→한아름조경` 12단계 조합 — **안 끊김**, 필터 정상(`주문 1건 · 품목 1줄`) ✅ |
| **잘못된 확장자** | `t.txt` 드롭 → `.csv · .xls · .xlsx 파일만 됩니다.` · 창 흔들림 ✅ |
| **`?v=`** | 5개 html **전부 `v=24`**, `v=23` 0개 ✅ |
| **로드 순서** | `… 08g → 08h → 08i → 09 …` ✅ · 버튼 **4개 그대로**, 라벨 `⬆ 주문 올리기` ✅ |

## §6 위험 6개 — 실측

| # | 결과 |
|---|---|
| 🔴① BOM | 실물 CSV 앞 `EF BB BF` 확인 → 머리글 27개 전부 매핑됨, 「못 찾은 칸」 안 뜸. **BOM 잘 뗐다** ✅ |
| 🔴② 옵션 8배 | `수량=4개` + M열 2 + 코드꼬리 `(3)` → **8개**. 24 가 아니다 ✅ |
| 🔴③ 중복이 진짜 주문을 삼킴 | 코드 빈칸 2줄(같은 주문 S-005, 다른 상품) → **둘 다 저장**, 재업로드 때 **둘 다 「이미」** ✅ |
| 🟡④ 우편 앞 0 | `03022`·`04524` 앞 0 생존, 빈칸은 `''` 유지 ✅ |
| 🟡⑤ 1970-01-01 | `5/13/26`→2026-05-13 · `2026-08-08 09:12:33`→2026-08-08 · `작년 여름`→**저장 안 하고 「발주일을 못 읽은 줄 1건」 상자** ✅ |
| 🟡⑥ 서비스 품목 | **해당 없음** — 로젠 파일은 올린 27칸으로만 만들어 v3 서비스 품목이 애초에 안 섞인다. 다만 「서비스 N줄 함께 나감」 안내도 없다 → 🟡-6 |

---

## 🟡 배포 컨펌 때 우람님께 함께 여쭐 것

**🟡-1 · 한 송장에 동봉카드가 두 장 나온다 — 둘 다 같은 번호**
`08a.묶음키`(이름+전화+주소) 와 로젠 그룹키(이름+우편번호+연락처) 가 다르다(계획서 §4 가 이미 경고한 그대로).
안심번호가 서로 다른 같은 사람(박도현) 2줄은 **로젠에선 1송장(9번)** 인데 **카드는 2장, 둘 다 「9」** 로 나왔다.
숫자가 틀린 건 아니지만 상자 하나에 카드가 두 장 들어간다.

**🟡-2 · 출력번호가 없는 주문이 섞이면 카드 번호가 겹친다**
`09:180` 의 되돌림값 `출력번호 || (i+1)` 이 문제다. 오늘 날짜만 걸어 카드를 뽑았더니
한서영(진짜 출력번호 **10**) 바로 뒤 수동주문 그룹이 `i+1` 로 **또 10** 이 됐다 — 실측 `1,2,5,6,7,8,9,9,10,10,11,…`.
전화주문·수동주문이 하루에 하나라도 있으면 겹칠 수 있다. (기간을 넓혀 전부 보면 안 겹쳤다 — 목록 구성에 따라 갈린다)

**🟡-3 · 시안에 있는 버튼 3개가 없다**
`시안/07-주문올리기-macA.html` 에 있는 **「전부 품목코드 지정하기」·「전부 그대로 두기」**(381·382행),
**「그래도 새로 넣기」**(409행), **「이 회차 되돌리기」**(506행) 가 구현에 없다.
넷째 것은 계획서가 「이번에 안 만든다」로 이미 정했지만(§우람님께 여쭐 것 4),
**「그래도 새로 넣기」가 없으면 잘못 지운 주문을 파일로 다시 넣을 길이 없다.**

**🟡-4 · 완료창 표 내용이 시안과 다르다**
시안 = 받는 분 / 박스 수 / 보내는 분 / 품목명 칸 / 파일 이름.
구현 = 줄 수 / 박스운임(J열) / 품목명 칸(E열) / 출력번호 / 파일 이름.
구현 쪽이 정보가 더 쓸모 있어 보이지만 **시안 컨펌본과 다르다.** 특히 **「보내는 분」이 빠졌다.**

**🟡-5 · 파일 이름이 시안과 다르다**
시안 `로젠_대량등록_20260808.xlsx` → 구현 `20260808-03.xlsx`.
구현이 계획서 §4(`20{묶음id}.xlsx`)를 따른 것이라 규칙 위반은 아니다. 어느 쪽으로 할지 한 번 정하시면 된다.
(계획서 §우람님께 여쭐 것 2 — 같은 회차를 두 번 받으면 크롬이 ` (1)` 을 붙이는 것도 그대로다. 실측: 두 번째 내려받기 때 새 파일이 안 떨어졌다)

**🟡-6 · 저장 안 된 줄도 로젠 파일에는 들어가는데, 그 줄엔 카드가 없다**
발주일을 못 읽은 줄(최유리)이 로젠 파일 **3번 송장**으로 나갔는데 v3 주문으로는 저장이 안 돼 **동봉카드는 안 만들어진다.**
「저장하지 않음 · 로젠 파일에는 들어감」이라고 화면에 적혀는 있다. 카드 번호가 3만 건너뛴다는 것까지는 안 적혀 있다.

---

## 🟢 잘 된 것 — 적어만 둔다

- **저장 버튼 전에는 저장소가 한 글자도 안 바뀐다.** 미리보기·재업로드·엉뚱한 파일 전부에서 `zg.v3.주문`·`주문묶음`·`출고` 줄 수를 재서 확인했다
- **큐가 새 줄만 싣는다.** 35줄짜리 표를 통째로 다시 쓰는데도 `보낼것` 에는 바뀐 12줄만 들어갔다 — `01c` 의 줄 대조를 제대로 태웠다
- **08f·08g·주문.css 를 진짜로 안 건드렸다.** shasum 으로 확인했고, 실물 출력완료 파일 결과가 8/6 검수 숫자와 글자 그대로 같다
- **정렬·묶기·박스점수를 손으로 계산한 값과 13줄 전부 대조했는데 한 자리도 안 틀렸다.** 안심번호 통일·우편 없는 줄 분리·확인요망 경계(13점)까지

---

## 시험한 방법 (다음 사람이 재현할 수 있게)

```bash
cd /Users/zerogardening/claude-projects/제로가드닝/통합관리 && python3 -m http.server 8778
# ../index.html?from= 게이트를 지나려면 로그인 토큰이 있어야 하고,
# 세션이 가짜면 01b:271 이 다시 튕긴다 → CDP 로 cdn.jsdelivr.net 을 막아 로컬 전용으로 떨군다
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --remote-debugging-port=9333 "--remote-allow-origins=*" --user-data-dir=/tmp/prof
```

- 시드는 로그인 상태면 안 심긴다(`01-저장소.js:192`) → `ZG.시드.심기()` 를 직접 부른다
- 파일은 base64 → `new File()` → `DataTransfer` → `DragEvent('drop')` 으로 `.pcsheet.dlv .drop` 에 떨군다
- 내려받기는 `Browser.setDownloadBehavior` 로 받아 `zipfile` + `sheet1.xml` 로 셀을 직접 읽는다
- 실물 대조 스크립트는 27칸을 열 이름(A~AA)으로 짝지어 전부 비교한다 — 화면 요약을 믿지 않는다
- 🔴 `백업/` 실물 두 파일은 **밖으로 복사하지 않았다.** 브라우저 메모리로만 넣었고, 그 결과 샷도 이 폴더에 안 남겼다

---
---

# 재검수 — 커밋 `3869436` (E2 원본유지 수정)

2026-08-08 15:0x · 검수자(checker) · **builder 자체보고를 안 믿고 처음부터 다시 돌렸다.**
같은 방법(실제 Chrome + CDP · 진짜 `File` 을 `DragEvent('drop')` · 내려받은 xlsx 를 `zipfile` 로 풀어 셀 직접 읽기).

## 판정 — 🔴 **0건** · 새 🟡 2건 → **통과** (배포 컨펌으로 넘긴다)

---

## 1 · 커밋이 정말 한 파일뿐인가 — ✅

```
git show --stat 3869436
 v3/js/08i-로젠파일.js | 11 ++++++++---   1 file changed, 8 insertions(+), 3 deletions(-)
```

바뀐 곳은 딱 두 군데다 — `원본그대로(것)` 함수 하나 새로 만들고, 170행의 앞머리 조건에 `&& !원본그대로(것)` 를 더했다.
`08h` · `09` · `08d` · `08b` · html 5개 · css **한 글자도 안 건드렸다.**
→ 지난번 🟡 6건(카드 번호 겹침 · 시안 버튼 3개 · 완료창 표 · 파일명 · 저장 안 된 줄 카드 없음)은 **손대지 않았다.** 그대로 남아 있다.

## 2 · 실물 짝 대조 — ✅ **통과 기준 그대로 맞췄다**

`백업/카페24주문원본_20260806.csv`(2줄)를 브라우저에 실제로 떨궈 → 미리보기 → 저장 → 내려받기.
나온 `20260808-01.xlsx` 와 `백업/로젠대량등록_20260806-01.xlsx` 를 27칸 × 2줄로 기계 대조:

```
시트명 송장출력 == 송장출력 · 행수 2 == 2 · 27칸 전부 문자열(t 속성 예외 0개)
  ✗ E1  기준=[(★2) 추명국 핑크터치(15cm) 1개]
        새것=[(1/★2) 추명국 핑크터치(15cm) 1개]      ← 계획서 §6-5 대로. 맞다
다른 칸 수: 1 / 54
```

**E2 가 원본과 한 글자도 안 다르다.** 지난번 붙었던 `(2) ` 가 사라졌다.
**53칸 전부 일치** — 통과 기준(`E1` 하나만 다름)을 정확히 만족한다.
`J=2750` · `L1=18100` `L2=18150` · `Z=0` · `O1=41007` `O2=03022`(앞 0 생존) 전부 그대로다.

> 재현 조건 — 실서비스에는 있고 목데이터엔 없는 `ANP01-15`(유통명 `추명국 핑크터치`) 품목과
> 곽상용/41007 의 지난 주문 1건을 localStorage 에 심어 **농장 상태를 재현한 뒤** 올렸다.
> 이걸 안 심으면 1행도 코드 못 찾음이 되어 E1 도 원본 그대로 나온다(그 자체는 정상 동작이다).
> 저장 전에는 `zg.v3.주문`·`주문묶음`·`출고` 가 한 줄도 안 움직였다(36/0/43 → 저장 후 38/1/43).

**콘솔 에러 0건 · 예외 0건.** (막아 둔 CDN 경고만)

## 3 · 회귀 — §7 표 6·7·8 다시 돌렸다

함정 CSV(13줄)로 처음부터: 시드 22 → 저장 34 · 묶음 3 · 출고 43 불변.

| # | 항목 | 결과 | |
|---|---|---|---|
| 6 | 로젠 내려받기 | `20260808-03.xlsx` **13줄 · 10묶음** · 시트명·머리글없음·27칸 문자열 그대로 · J열 확인요망 3묶음 · 정렬 순서 지난번과 동일 | ✅ |
| 8 | 한 번 더 (재업로드 → 재발행 → 내려받기) | 「이미 12건 / 들어올 0건」 · 주문 34 그대로 · `[id,출력번호]` 34줄 **완전 동일**(`출력번호 안 바뀜? True`) | ✅ |
| — | 출력번호 되쓰기 | 코드 없는 줄에도 `출력번호` 는 **여전히 저장된다**(정민수=5 · 오지훈=7 · 한서영=10). **데이터가 날아가지 않았다** | ✅ |
| 7 | 동봉카드 짝 | 카드 14장 번호 `1,2,5,6,7,8,9,9,10,10,11,12,13,14` — 그런데 **E열엔 `(5)`·`(6)`·`(7)`·`(10)` 이 아예 없다** | ⚠️ 🟡-7 |

E열 실측(13줄):

```
1  (1) 휴케라 '팰리스퍼플'(15cm) 1개     H=HEP01-15
2  (2) 층꽃 '스털링실버'(18cm) 1개       H=CAS01-18
3  (3) 청사초(10cm) 1개                  H=CAR01-10
4      제로가드닝 에키네시아 판테온      H=ech01-10          ← 코드 못 찾음 → 번호 없음
5      제로가드닝 루엘리아               H=Ruellia simplex   ← 번호 없음
6      제로가드닝 루드베키아 2입         H=ERB01-10(2)       ← 번호 없음
7      제로가드닝 에키네시아 4입         H=ECH01-10(3)       ← 번호 없음
8  (8) 억새(27cm) 3개/                                       ← 합포장 첫 줄에 코드 있음 → 번호 있음
9      털수염풀(10cm) 1개/
10 (9) 수크령 '레드헤드'(15cm) 1개/
11     청사초(10cm) 1개/
12     제로가드닝 무늬사초 아우레올라/   H=없음              ← 송장에 번호가 통째로 없다
13     제로가드닝 억새 레드치프/         H=없음
```

## 4 · 새 🟡 — 배포 컨펌 때 함께 여쭐 것

**🟡-7 · 코드 없는 송장은 로젠 파일에 번호가 안 찍힌다 — 카드에는 찍힌다**
이건 **고쳐서 생긴 게 아니라 §0(원본 유지)을 골랐기 때문**이다. 지난 🔴 보고서가 예고한 그 갈림길이고,
채점표 §7 240행이 「2행은 코드 없어 원본 유지」로 못 박았으니 **builder 선택이 맞다.**
다만 §4·§7-7 의 「카드 번호와 E열 `(N)` 이 한 자도 안 다름」은 **코드 없는 송장에서만 성립하지 않는다.**
포장하시는 분이 카드 `5` 를 들고 송장에서 `(5)` 를 못 찾는다. **번호를 어디에 적을지 우람님이 정하실 일이다.**
(선택지 — ① 지금대로 둔다 ② 그 송장엔 카드를 안 만든다 ③ E열 말고 다른 빈 칸에 번호를 넣는다)

**🟡-8 · 합포장 첫 줄만 코드가 없으면 송장 전체가 번호를 잃는다 — 뒷줄에 코드가 있어도**
직접 만든 2줄 CSV(같은 사람 · 1줄째 코드 없음 · 2줄째 `HEP01-15`)로 재현했다.

```
1  제로가드닝 코드없는상품/        H=없음     ← 여기에 (1) 이 붙어야 하는데 안 붙는다
2  휴케라 '팰리스퍼플'(15cm) 1개/  H=HEP01-15
저장된 출력번호 = 1 (두 줄 다)                ← 카드는 「1」로 나온다
```

`08i:171` 의 `j === 0` 이 「묶음 맨 앞 줄」만 보기 때문이다.
**앞머리를 「코드가 있는 첫 줄」로 옮기면** 원본 유지(§0)도 지키고 번호(§4)도 살릴 수 있다 — 🟢 제안이다.
(🟡-7 을 우람님이 ①로 정하시면 이건 같이 정리하면 된다)

**🟡-9 · `?v=` 를 안 올렸다 (배포 전 필수)**
`js/08i-로젠파일.js` 를 고쳤는데 html 5개가 전부 `?v=24` 그대로다(개발지침 §3).
지금은 파일 하나짜리 수정이라 화면이 깨지지는 않지만, **캐시에 옛 08i 를 든 브라우저에선 이 수정이 아예 안 먹는다.**
push 전에 `v=25` 로 올려야 한다.

## 5 · 지난 🟡 6건 — 그대로 살아 있다 (재확인 대상 아님)

🟡-2(출력번호 없는 주문이 섞이면 카드 번호 겹침)는 이번 실측에서도 그대로 보였다 — `…9,9,10,10,11,12,13,14`.
나머지 5건도 건드린 파일이 없으니 변함없다.

## 6 · 이번에 돌린 것

```bash
python3 -m http.server 8778                       # 통합관리/ 에서
Chrome --remote-debugging-port=9334 --user-data-dir=…/prof
# CDP 로 cdn.jsdelivr.net 차단 → 로컬 전용. 게이트(01b:37)는 sb-…-auth-token 을 심어 통과
```

- 대조 스크립트는 A~AA 27칸을 이름으로 짝지어 **54칸 전부** 비교한다. 화면 문구는 안 믿었다
- 🔴 `백업/` 실물 파일은 **밖으로 복사하지 않았다.** 브라우저 메모리로만 넣었고 샷도 안 남겼다

---
---

# 재검수 ② — 커밋 `151c0d6` (앞머리를 「코드 있는 첫 줄」로)

2026-08-08 16:0x · 검수자(checker) · **builder 자체보고를 안 믿고 처음부터 다시 돌렸다.**
실제 Chrome + CDP · 진짜 `File` 을 `DragEvent('drop')` · 내려받은 xlsx 를 `zipfile` 로 풀어 셀 직접 읽기.
샷 — `검수/샷-2단계보완-재검수/`

## 판정 — 🔴 **0건** · 🟡 **0건 새로 생김** → **통과** (배포 컨펌으로 넘긴다)

---

## 1 · 커밋 범위 — ✅

```
git show --stat 151c0d6
 v3/index.html · v3/소싱.html · v3/업체.html · v3/이사.html · v3/주문.html · v3/js/08i-로젠파일.js
 6 files changed, 95 insertions(+), 92 deletions(-)
```

- html 5개의 diff 를 `?v=25|26` 줄만 빼고 걸러 봤더니 **남는 줄이 0** — 캐시버전 말고는 한 글자도 안 건드렸다
- `grep '?v=25'` → **0건.** 다섯 파일의 `?v=26` 개수 = index 20 · 소싱 15 · 업체 19 · 이사 8 · 주문 26.
  (`icons/icon-192.png?v=2` 두 줄은 아이콘용으로 원래 있던 것이다)
- shasum 무변경 확인 — `08f-배송완료.js` · `08g-배송완료창.js` · `08h-주문올리기.js` · `09-동봉카드.js` · `css/주문.css` **전부 SAME**
- `08i` 실변경은 두 군데뿐이다 — `머리자리` 를 `some()` 으로 먼저 찾고, `j === 0 && !원본그대로(것)` → `j === 머리자리`
- 브라우저가 실제로 받은 스크립트: `js/08i-로젠파일.js?v=26` (CDP 로 확인)

## 2 · 실물 짝 대조 — ✅ **통과 기준 그대로**

`백업/카페24주문원본_20260806.csv`(2줄)를 실제로 떨궈 → 미리보기 → 저장 → 내려받기.
나온 `20260808-03.xlsx` 와 `백업/로젠대량등록_20260806-01.xlsx` 를 27칸 × 2줄 기계 대조:

```
시트명 송장출력 == 송장출력 · 행수 2 == 2 · 내려받은 27칸 전부 t="str"(54/54)
  X E1  기준=[(★2) 추명국 핑크터치(15cm) 1개]
        새것=[(1/★2) 추명국 핑크터치(15cm) 1개]     ← 계획서 §6-5 대로. 맞다
다른 칸 수: 1 / 54
```

**E2 는 원본과 한 글자도 안 다르다**(김연숙 · 코드 없음 · 혼자 1줄 묶음 → 앞머리 없음이 정상).
**53칸 전부 일치.** `J=2750` · `L1=18100` `L2=18150` · `O2=03022`(앞 0 생존) · `Z=0` 그대로.
저장 전 주문 23 → 저장 후 25 · 묶음 2 → 3. **콘솔 에러 0.**

## 3 · 🟡-8 이 정말 고쳐졌나 — ✅ (직접 만든 2줄 CSV)

같은 수령인·우편번호·연락처(장미래 / 12345 / 010-1111-2222)로 묶이게 만든 `caseA.csv` —
**1줄째 코드 없음 · 2줄째 `HEP01-15`**.

```
1  E=[제로가드닝 코드없는상품/]                H=[]           ← 원본 그대로. 앞머리 없음 ✅
2  E=[(1) 휴케라 '팰리스퍼플'(15cm) 1개/]      H=[HEP01-15]   ← 여기에 (1) 이 붙었다 ✅
저장된 출력번호 = 1 (두 줄 다)
```

지난번엔 두 줄 다 번호가 없었다. **이제 코드 있는 둘째 줄이 번호를 든다.**
코드 없는 첫 줄은 카페24 상품명 그대로다(뒤 `/` 는 합포장 표시라 원래 붙는 것이다).

## 4 · 묶음 전체가 코드 없을 때 — 그대로 (우람님 감수)

`caseB.csv`(노코드 2줄, 둘 다 코드 없음):

```
1  E=[제로가드닝 무코드 하나/]   H=[]
2  E=[제로가드닝 무코드 둘/]     H=[]
저장된 출력번호 = 1 (두 줄 다)   ← 카드는 「1」로 나온다
```

**아무 줄에도 안 붙는다.** 우람님이 감수하기로 하신 그대로다 — 🟡-7 의 남은 절반이고 새 문제가 아니다.

## 5 · 출력번호 되쓰기 — ✅ 코드 유무와 무관하게 전 줄에 저장된다

함정 CSV(13줄) 저장 뒤 실측:

```
이수진(-)=4 · 정민수(-)=5 · 박준호(-)=6 · 오지훈(-)=7 · 한서영(-)=10 · 한서영(-)=10
서지우(HEP01-15)=1 · 윤하늘(CAS01-18)=2 · 박도현=9,9 · 정하늘=8,8
```

**코드 없는 줄에도 출력번호가 그대로 들어간다. 데이터가 날아가지 않았다.**

## 6 · 함정 CSV 13줄 E열 실측 — 합포장 8·9·10·13행

실제 UI 로 올려 저장하고 내려받은 `20260808-03.xlsx` 13줄 전부:

```
 1 (1) 휴케라 '팰리스퍼플'(15cm) 1개     H=HEP01-15   J=2750
 2 (2) 층꽃 '스털링실버'(18cm) 1개       H=CAS01-18   J=2750
 3 (3) 청사초(10cm) 1개                  H=CAR01-10   J=2750     (최유리 — 발주일 「작년 여름」이라 저장은 안 됨)
 4     제로가드닝 에키네시아 판테온      H=ech01-10   J=2750     ← 코드 못 찾음 → 원본 유지
 5     제로가드닝 루엘리아               H=Ruellia…   J=2750
 6     제로가드닝 루드베키아 2입         H=ERB01-10(2) J=확인요망
 7     제로가드닝 에키네시아 4입         H=ECH01-10(3) J=확인요망
 8 (8) 억새(27cm) 3개/                   H=MIS01-27   J=확인요망  ← 정하늘 합포장, 첫 줄에 코드 → (8)
 9     털수염풀(10cm) 1개/               H=FES01-10   J=확인요망
10 (9) 수크령 '레드헤드'(15cm) 1개/      H=PER03-15   J=2750     ← 박도현 합포장(안심번호) → (9)
11     청사초(10cm) 1개/                 H=CAR01-10   J=2750
12     제로가드닝 무늬사초 아우레올라/   H=(없음)     J=2750     ← 한서영 — 묶음 전체 코드 없음 → 번호 없음
13     제로가드닝 억새 레드치프/         H=(없음)     J=2750
```

**8·10행은 지난번과 같다**(원래 첫 줄에 코드가 있던 묶음이라 이번 수정과 무관).
**12·13행은 여전히 번호 없음** — §4 그대로다. 13줄 · 10묶음 · 확인요망 3묶음, 정렬 순서 지난번과 동일.

## 7 · 동봉카드 짝 — 지난번과 완전히 같다 (변화 없음)

실제 화면의 `💌 동봉카드` 버튼을 눌러 `.enno` 를 읽었다:

```
카드 14장 번호 = 1,2,5,6,7,8,9,9,10,10,11,12,13,14
E열에 있는 번호 = (1) (2) (3) (8) (9)
```

지난 재검수와 **한 글자도 다르지 않다.** 5·6·7·10 이 카드에만 있는 건 🟡-7(코드 없는 송장)이고,
11~14 는 🟡-2(출력번호 없는 옛 주문이 섞여 인덱스로 대체)다. **둘 다 이번에 안 건드린 기존 항목이다.**

## 8 · 재발행 회귀 — ✅

같은 함정 CSV 를 두 번째로 올렸다.

| 항목 | 결과 |
|---|---|
| 미리보기 | 「들어올 줄 0건 / 이미 있는 줄 12건」 |
| 내려받은 27칸 × 13줄 | **1회차 파일과 완전 동일** (`True`) |
| `[id, 출력번호]` 전 줄 스냅샷 | **완전 동일** (`출력번호 안 바뀜? True`) |

## 9 · 콘솔 — ✅ 에러 0

화면 열기 · 드롭 · 미리보기 · 저장 · 내려받기 · 카드 열기 전 구간에서
`Log.entryAdded(error)` · `Runtime.exceptionThrown` · `console.error` **전부 0건.**
(막아 둔 `cdn.jsdelivr.net` 경고만)

## 10 · 한글 조합 — ✅

주문 조회 검색칸(`이름 · 전화번호 · 주소 · 상품명`)에 CDP `Input.imeSetComposition` 으로
ㅎ→후→휴→휴ㅋ→휴케→휴케ㄹ→휴케라 를 실제 조합해 넣었다. 최종값 `'휴케라'` — **조합이 안 끊긴다.**
(이번 수정은 입력칸을 건드리지 않았지만 캐시버전을 올려 다섯 화면 js 를 통째로 다시 받게 했으므로 확인했다)

## 11 · 다시 돌린 방법

```bash
python3 -m http.server 8778                        # 통합관리/ 에서
Chrome --remote-debugging-port=9335 --user-data-dir=…/prof
# CDP 로 cdn.jsdelivr.net 차단 → 로컬 전용. 게이트(01b:37)는 sb-…-auth-token 을 심어 통과
# 시드 뒤 ANP01-15(추명국 핑크터치)·HEP01-15 품목과 곽상용/41007 지난 주문 1건을 심어 농장 상태 재현
```

- 대조 스크립트는 A~AA 27칸을 이름으로 짝지어 **전 칸** 비교한다. 화면 문구는 안 믿었다
- 🔴 `백업/` 실물 두 파일은 **밖으로 복사하지 않았다.** 브라우저 메모리로만 넣었다
