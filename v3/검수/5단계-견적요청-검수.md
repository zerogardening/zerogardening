# 5단계 검수 — 주문 관리 › 견적 요청

# ▣ 재검수 (2026-08-05, 🟡2·🟡3 수정분) — 🔴 0건 → **통과**

- 대상 `js/15c-견적폰.js`(250) `js/15d-견적탭.js`(86) · `주문.html` `?v=8`
- 방법 — `python3 -m http.server 8777` + playwright **headed 크롬(실제 창)**. 폰 390×844 / PC 1440×900.
  실데이터 6건(`백업/…2026-08-02.json` `quoteRequests`)을 `99-이사.js` 규칙 그대로 넣고 확인. 한글은 CDP `Input.imeSetComposition` 조합 타이핑.
- 스크린샷 `검수/샷-5단계-재검수/` 14장

| # | 확인한 것 | 결과 |
|---|---|---|
| 1 | **폰 상세에서 편집 중 상태 토글** | 내용칸 끝에 `확인함`(IME 조합), 연락처 끝에 `9`를 넣은 채 `완료→취소→요청→완료` **네 번** 토글 — 매번 **내용·연락처 그대로 유지**. 칩만 바뀐다 |
| 1 | 토글은 즉시저장, 내용은 아직 아님 | 매 토글마다 localStorage `상태`가 `완료/취소/요청/완료`로 즉시 반영. 같은 시점 저장소 `내용`에는 `확인함` **없음**(= 저장 눌러야 반영) |
| 1 | 저장 → 새로고침 | `저장` 뒤 내용 `…견적 요청확인함휴케ㄹ휴케라` · 연락처 `010-9490-04829` · 상태 `요청`(마지막 토글값) 그대로. F5 뒤에도 동일, 목록 카드에도 같은 값 |
| 2 | **조합 중에 토글** | `ㅎ→휴→휴ㅋ→휴케→휴케ㄹ` 조합(밑줄 표시 상태)에서 `요청` 칩 클릭 → 조합 글자 **안 깨짐**(`휴케ㄹ` 그대로, `ㅎㅠㅋㅔㄹ` 분해 없음). 이어서 조합 마무리하니 `휴케라` 정상 확정 |
| 3 | **등록 화면 서브탭** | `.subtabs` 0개 · 문서 전체 `[role=tab]` **0개** — 탭으로 이탈할 경로가 없다. 나가는 길은 라벨 붙은 `‹ 견적 요청` 뒤로가기뿐 |
| 3 | **상세 화면 서브탭** | `.subtabs` 0개 |
| 3 | 목록 화면 | 서브탭 **그대로 1줄·버튼 2개**(`주문 조회`·`견적 요청`), 카드 6장. 시안대로 |
| 4 | 버전 | `15a~15d` 네 개 모두 `?v=8` (실측 `src` 문자열) |
| 4 | 콘솔 | 폰·PC 전 구간 **error 0건 · pageerror 0건** |

**회귀(짧게)** — PC 견적 표 6줄 → 상태 드롭다운 `완료` 즉시저장(저장소 1건 완료) · 검색 `한울`(IME) → 1줄 · 삭제 확인창 뒤 6→5줄 · `주문 조회` 복귀 시 표 7줄 정상 + 머리줄 버튼 `hidden:false` 복원 + 길안내 `주문 › 주문 조회`.
폰도 검색 `한울` → 카드 1장, `주문 조회` 복귀 정상.

**1차 🟡 처리** — 🟡2(편집 중 소실)·🟡3(안쪽 화면 탭 이탈) **해소**. 🟡1(폰 상세에 요청일 없음)·🟡4(`🖥 PC화면으로` 없음)는 그대로 남아 배포 컨펌 때 함께 여쭐 항목이다.

**재검수에서 새로 적어 두는 🟢**
1. 상태를 연달아 누르면 토스트가 **겹쳐 쌓인다**(4장까지 확인). 잠깐 뒤 사라지지만 그동안 삭제 버튼을 덮는다.
2. 토글해도 요약줄의 `수정 05-11`은 그 자리에 머문다(다시 그리지 않으므로). 저장값은 갱신되니 표시만의 문제다.
3. 폰 등록 화면에서 값을 넣다 `‹ 견적 요청`을 누르면 초안이 경고 없이 사라진다(다시 `＋`를 누르면 빈 폼). 라벨이 붙은 의도적 이탈이라 이번엔 그대로 둔다.

---


- 검수일 2026-08-05 · 검수자 dev-checker
- 대상 `js/15a-견적자료.js`(97) `js/15b-견적PC.js`(269) `js/15c-견적폰.js`(245) `js/15d-견적탭.js`(82) · `js/08b-주문목록.js`(2줄) · `css/주문.css`(뒤 118줄) · html 5개 `?v=7`
- 방법 — `python3 -m http.server 8777` + playwright **headed 크롬(실제 창)**, 실제 클릭·CDP IME 한글 조합 타이핑. PC 1440×900 / 폰 390×844.
  실데이터 6건은 `백업/제로가드닝_백업_2026-08-02.json`의 `quoteRequests`를 `99-이사.js`와 같은 규칙으로 넣고 확인했다(시드에는 견적 데이터가 없다 — `?reset=1`만으로는 0건이다).
- 스크린샷 `검수/샷-5단계-견적요청/` 43장

## 판정 — 🔴 0건 · 🟡 4건 · 🟢 3건 → **통과**

계산식·코드 규칙 위반 없음 · 화면 정상 · 데이터 유실 없음 · 콘솔 에러 0건(PC·폰·회귀 전 구간).

---

## 1. 채점표 대조 (`기능명세.md` 113~123줄, 🛒 주문 관리)

| 명세 | 결과 |
|---|---|
| **견적 요청** — 요청일자* · 고객명* · 연락처 · 요청내용* 등록 | ✅ 4칸 그대로. 필수 3칸 비우면 저장 안 되고 흔들림 + 토스트(`고객명을 적어 주세요` 실측) |
| → 목록 | ✅ 6건, `요청일` 내림차순 → 같은 날은 `등록일시` 내림차순 (05-11 4건 → 05-08 → 05-07) |
| 상태: 요청/완료/취소 | ✅ 셋뿐. PC 드롭다운 즉시저장 · 폰 3칸 토글 즉시저장 |

명세에 적힌 견적 요청 기능은 **빠진 것이 없다.**
(명세 「주문 관리 탭 3개」 중 **대시보드**는 v3에 아직 없다 — 5단계 범위 밖, 별건으로 남는다.)

## 2. 업무규칙 대조

견적 요청은 가격 공식·품목코드·규격 정규화·재고 계산과 **닿지 않는 독립 메모**다(설계 §11). 계산 규칙 위반 대상 없음.
확인한 규칙 사항 — 상태값이 셋 밖(`보류`)인 데이터는 화면에서 `요청`으로 그리되 **저장값을 몰래 고치지 않는다**(실측: 수정 저장 뒤에도 `보류` 유지). 삭제는 실제 삭제, 상쇄 레코드 없음.

## 3. PC (1440×900)

| 확인 | 결과 |
|---|---|
| 탭 두 칸 | `주문 조회` · `견적 요청` — `pc-head` 아래, 시안 위치 그대로 |
| 목록 6건 | 정렬·연락처·내용 모두 시안과 일치. `td.memo{white-space:pre-wrap}` — 잣나무 건의 빈 줄까지 그대로 |
| 길안내 | `주문 관리 › 견적 요청`, 수정 중엔 `… › 수정` |
| 머리줄 버튼 | 견적 탭에서 `hidden`(카페24·로젠·출고·동봉카드), 주문 조회로 돌아오면 **다시 보인다**(실측 `hidden:false`) |
| 통계 4칸 | 전체6/요청5/완료0/취소1. **상태 필터 「취소」(1건 표시) 중에도 통계는 전체 기준 6** — 시안대로 |
| 검색 | 고객명·내용·연락처 부분일치. `한울`→1건 · `함안`→3건 · `9490`(숫자만)→1건 |
| 상태 즉시저장 | 드롭다운 변경 → 저장 버튼 없이 localStorage 반영 + `수정일시` 갱신 + F5 뒤에도 `완료` 유지 |
| 등록 | 표 맨 위에 뜸(`2026-08-05 김철수`), `id = q_1785923804775_0op652` (`q_`+ms+`_`+6자) · 상태 `요청` · 등록/수정일시 동일 |
| 수정 | **인라인 카드**로 올라옴. `window.prompt` 호출 0회(코드에도 없음). 대상 줄 `tr.editing` 초록 + 상태칸 `수정 중` 칩. 저장하면 등록 모드 복귀 |
| 삭제 | `askbox` 확인창 `되돌릴 수 없습니다` — 취소/Esc면 안 지워지고, `삭제`를 눌러야 지워짐 |
| 쪽나눔 | 45건 넣어 확인 — 20/20/5, 3쪽 이동 정상, 검색하면 1쪽으로 복귀 |
| 빈 상태 | 0건 `아직 접수한 견적 요청이 없습니다` · 검색 0건 `찾는 견적 요청이 없습니다` |
| 안전 | 고객명 `<img src=x onerror=…>` 넣어도 텍스트로만 출력, 스크립트 실행 안 됨 |

## 4. 한글 직접 타이핑 (CDP IME 조합 — 8/4 「휴케라」 사고 재발 확인)

조합 단계(`ㅎ→하→한`)를 실제로 보내고 마지막에 확정했다. **깨진 칸 0곳.**

| 칸 | 친 글 | 실제 값 |
|---|---|---|
| PC 검색창 | 한울 | `한울` (결과 1건) |
| PC 검색창 | 함안 | `함안` (결과 3건) |
| PC 고객명 | 김철수 | `김철수` |
| PC 요청 내용 | 소나무 | `소나무 3` |
| PC 수정 중 내용 | 다섯그루 | `소나무 3다섯그루` (저장값 확인) |
| 폰 검색창 | 함안 | `함안` (카드 3장) |
| 폰 고객명 | 박영희 | `박영희` |
| 폰 요청 내용 | 느티나무 / 메모 | `느티나무 5` → `느티나무 5메모` |

## 5. 폰 (390×844)

- 가로 오버플로 **0** — 목록·등록·상세 전부 `scrollWidth 390 = innerWidth 390`.
- 카드 목록 7장(연락처 없는 시험 건 포함), 상태칩·`MM-DD`·`pre-wrap` 내용·연락처.
- `📞 전화` — 숫자 8자리 이상인 6건에만 뜨고 `tel:01094900482` 형식. **연락처 없는 건에는 안 뜬다.**
- 상태 3칸 토글 — 누르는 즉시 저장(localStorage `완료`), F5 뒤에도 유지.
- 새 견적 등록 → 목록 복귀, 저장 레코드 정상. 필수칸 비우고 저장하면 토스트 `고객명을 적어 주세요`.
- 삭제 — 확인창 `취소`면 8건 유지, `삭제`면 7건.
- 아래 고정 탭바에 가리는 곳 없음(목록·등록·상세 맨 아래까지 스크롤해 실측: 콘텐츠 bottom 748 < nav top 775).
- F5 하면 주문 조회로 돌아온다(설계대로 탭 상태는 저장하지 않는다).

## 6. 🔴 회귀 — 이상 없음

| 화면 | 결과 |
|---|---|
| PC 주문 조회 | 표 6줄 + 총계줄 그대로, 기간칩·회차칩·검색·엑셀·쪽나눔 정상. 탭줄은 하나만 생긴다(중복 삽입 없음) |
| 카페24 → 로젠 파일 / 로젠 → 배송완료 파일 | 클릭 시 파일 고르개 정상, 에러 0 |
| 출고리스트 · 동봉카드 | `.pcsheet` 창 정상 개폐 |
| 폰 주문 · 수동주문 · 출고리스트 · 주문 상세 | 정상. 안쪽 뷰에는 서브탭이 안 붙는다(설계 §3-1 대로) |
| `index.html`(입고·재고) · `업체.html`(등록·검색·명세서) · `소싱.html`(키워드검색·상품분류) | 전부 정상, 콘솔 에러 0 |

## 7. 버전·저장키

- `grep -n '?v=' v3/*.html \| grep -v 'v=7'` → **출력 없음**. 다섯 파일 모두 `?v=7`.
- 실제 네트워크 응답으로 `15a~15d …?v=7` 네 개 로드 확인. `?v=` 없는 자원은 벤더 `lib/xlsx.min.js` 하나뿐(원래부터).
- localStorage 키 — `zg.v3.견적요청` 그대로 쓴다. **새 키 0개** (`재고조정·견적요청·설정·주문·품목·업체·출고·주문묶음·입고` — 5단계 전과 동일).
- 새로고침 후 데이터 유지 확인(등록·수정·상태변경 모두).
- 쓰기는 전부 `덧붙이기/바꾸기/지우기` 한 건 단위 — `전체쓰기` 호출 없음(코드 확인).

---

## 🟡 (배포 컨펌 때 함께 여쭐 것 — 따로 멈추지 않는다)

1. **폰 상세에 「요청일」이 어디에도 안 보인다.** 시안은 요약줄 왼쪽이 `요청일 2026-05-08`인데, 설계 §10에서 그 자리를 뒤로가기(`‹ 견적 요청`)로 바꾸면서 요청일이 화면에서 빠졌다. 폰에서 접수일을 아는 길은 목록 카드의 `05-08`뿐이다. (오른쪽 `수정 08-05`는 남아 있다)
2. **폰 상세에서 고치던 내용이 상태 토글 한 번에 되돌아간다.** 실측 — 내용칸에 `아직 저장 안 한 메모`를 넣고 `완료`를 누르면 화면이 다시 그려지며 원래 내용으로 복귀(경고 없음). 저장 규칙 위반은 아니지만(상태만 저장된 것은 맞다) 밭에서 쓰면 적던 메모를 잃는다.
3. **폰 등록·상세 화면에도 서브탭이 그대로 보인다.** 시안 안쪽 화면에는 탭줄이 없다. 등록 도중 `주문 조회`를 눌렀다 돌아오면 적던 고객명·내용이 빈칸이 된다(실측: `['2026-08-05','','','']`).
4. 시안 폰 상단 `🖥 PC화면으로`가 없다 — 설계 §10이 밝힌 대로 v3 전체에 아직 없는 기능이다(4단계에서 이미 컨펌 대기로 올라간 항목, 여기서도 같은 상태).

## 🟢 (적어만 둔다)

1. `주문.css` 뒤에 붙인 `select.inp`·`.stat`·`.empty`·`.st`가 전역 선택자라 **수동주문 화면의 화분 사이즈 드롭다운 모양도 함께 바뀐다**(화살표가 붙는다). 보기엔 오히려 통일돼 문제 없다. 설계 §8이 미뤄 둔 공통.css 통합 때 함께 정리하면 된다.
2. PC 표가 `thead` 없이 `<table>` 바로 아래 `th` 줄이다. 화면·읽기에는 지장 없다.
3. PC에서 등록 폼을 쓰다가 다른 줄의 `수정`을 누르면 쓰던 내용이 그 줄 값으로 덮인다. 견적 화면 성격상 흔치 않아 그대로 둬도 된다.
